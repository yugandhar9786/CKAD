apiVersion: v1
kind: Pod
metadata:
  name: resultapp-pod
  labels:
    name: resultapp-pod
    app: demo-voting-app
spec:
  containers:
  - name: resultapp-pod
    image: kodekloud/examplevotingapp_result:v1
    ports:
      - containerPort: 80
